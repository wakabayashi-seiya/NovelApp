<% provide(:title, "連載中の小説") %>

<h1>連載中の小説</h1>

<% if @novels.any? %>
  <div class="col-md-10 col-md-offset-1">
    <h3>小説 (<%= @novels.count %>)</h3>
    <ul class="novels">
      <%= render @novels %>
    </ul>
  </div>
<% else %>
  <h3>連載中の小説はありません。</h3>  
<% end %>