<% provide(:title, "完結済みの小説") %>

<h1>完結済みの小説</h1>

<% if @novels.any? %>
  <div class="col-md-10 col-md-offset-1">
    <h3>小説 (<%= @novels.count %>)</h3>
    <ul class="novels">
      <%= render @novels %>
    </ul>
  </div>
<% else %>
  <h3>完結済みの小説はありません。</h3>  
<% end %>