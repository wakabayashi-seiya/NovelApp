<% provide(:title, "完結済みの小説") %>

<h1>完結済みの小説</h1>

<div class="row justify-content-center">
  <div class="col-md-10">
    <% if @novels.any? %>
      <h3>小説 (<%= @novels.count %>)</h3>
      <ul class="novels">
        <%= render @novels %>
      </ul>
    <% else %>
      <h3>完結済みの小説はありません。</h3>  
    <% end %>
  </div>
</div>