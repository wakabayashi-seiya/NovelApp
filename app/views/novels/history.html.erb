<% provide(:title, "歴史・伝奇の小説") %>

<h1>歴史・伝奇の小説</h1>

<div class="row justify-content-center">
  <div class="col-md-10">
    <% if @novels.any? %>
      <h3>小説 (<%= @novels.count %>)</h3>
      <ul class="novels">
        <%= render @novels %>
      </ul>
    <% else %>
      <h3>まだこのジャンルの小説は投稿されていません。</h3>  
    <% end %>
  </div>
</div>